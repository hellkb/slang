struct Abc {
    a: i32,
    b: f32,
}

fn main() {
    let x = b[19] + a(1, 2);
    let c = 'v';

    //let array = [1, 2, 3, 4, 6];
    //let a: i32 = if x { 7 } else { 8 };
    //println(a);
    //print([123, 66])
}
